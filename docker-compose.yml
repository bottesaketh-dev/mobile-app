version: '3.8'

services:
  web:
    build: .
    container_name: restaurant_flask_web
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
